#include <stdio.h>
#include <stdib.h>
#include <signal.h>
#include <sys/types.h>
#include <errno.h>
/**
 * 
 *
 * 
 */
int main(void)
{
	pid_t children;
	int status;
	int count;
	if (children = -1)
{
	perror("Error:");
	return (0);
}
	wait
